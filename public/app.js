const $$=(q,ctx=document)=>[...ctx.querySelectorAll(q)];
$$('form[data-lead]').forEach(form=>form.addEventListener('submit',async e=>{e.preventDefault();const btn=form.querySelector('button[type=submit]');const toast=form.querySelector('.toast');const data=Object.fromEntries(new FormData(form).entries());data.services=$$('input[name="services"]:checked',form).map(x=>x.value);btn.disabled=true;btn.textContent='Envoi…';try{const r=await fetch(form.action,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const out=await r.json();if(!r.ok)throw new Error(out.error||'Erreur');toast.style.display='block';toast.textContent=out.message||'Merci, votre demande a bien été reçue.';form.reset()}catch(err){toast.style.display='block';toast.style.background='#fff0f0';toast.style.color='#9b1c1c';toast.textContent='Impossible d’envoyer pour le moment. Réessayez dans quelques instants.'}finally{btn.disabled=false;btn.textContent=btn.dataset.label||'Envoyer ma demande'}}));
$$('[data-scroll]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');if(id&&id.startsWith('#')){e.preventDefault();document.querySelector(id)?.scrollIntoView({behavior:'smooth'})}}));
const counters=$$('[data-count]');if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){const el=e.target,t=+el.dataset.count;let n=0;const step=Math.max(1,Math.ceil(t/40));const timer=setInterval(()=>{n=Math.min(t,n+step);el.textContent=n+(el.dataset.suffix||'');if(n>=t)clearInterval(timer)},28);io.unobserve(el)}}));counters.forEach(x=>io.observe(x))}
